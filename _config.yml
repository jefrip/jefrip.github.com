# Chirpy theme config — https://chirpy.cotes.page
# NOTE: Cloudflare Pages builds Jekyll via the github-pages gem, which does
# not ship jekyll-theme-chirpy. remote_theme fetches it from GitHub at build
# time (works on Cloudflare; jekyll-remote-theme is in github-pages whitelist).
remote_theme: cotes2020/jekyll-theme-chirpy@v7.6.0
lang: en
timezone: Asia/Jakarta
title: "It's me Jefri"
tagline: "Jefri's Blog"
description: "Personal blog of Jefri Pakpahan — software engineering, databases, and tech notes."
url: "https://jefri-p.com"
baseurl: ""
github:
  username: jefrip
twitter:
  username: jefriip
avatar: "/assets/img/avatar.png"

# Build
permalink: /:year/:month/:title/
paginate: 5
paginate_path: "/page/:num"
markdown: kramdown
highlighter: rouge
kramdown:
  syntax_highlighter: rouge

collections:
  tabs:
    output: true
    sort_by: order

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/

plugins:
  - jekyll-remote-theme
  - jekyll-sitemap
  - jekyll-paginate
  - jekyll-archives
  - jekyll-redirect-from
  - jekyll-include-cache

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - scripts/
  - docs/
  - .ruby-version

# Copy underscore-prefixed files to _site (Cloudflare Pages redirects)
include:
  - _redirects
